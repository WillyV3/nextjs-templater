# Generated by distui - https://github.com/WillyV3/nextjs-templater
# Edit as needed, distui won't overwrite manual changes

version: 2

before:
  hooks:
    - go test ./...
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    binary: nextjs-templater

archives:
  - format_overrides:
      - goos: windows
        format: zip
    files:
      - none*

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

brews:
  - name: github.com/WillyV3/nextjs-templater
    repository:
      owner: willyv3
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
    homepage: https://github.com/WillyV3/nextjs-templater
    description: "github.com/WillyV3/nextjs-templater"
    license: MIT
    directory: Formula
    commit_author:
      name: distui
      email: distui@users.noreply.github.com
    commit_msg_template: "Brew formula update for github.com/WillyV3/nextjs-templater version {{ .Tag }}"
    test: |
      system "#{bin}/github.com/WillyV3/nextjs-templater", "--version"

